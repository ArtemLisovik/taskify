import {WishItem} from 'features/WishItem/ui/WishItem'

import './WishList.scss'

export const WishList = () => {

    // const wishList = 
    return(
        <div className="wishList">
            <WishItem/>
            <WishItem/>
            <WishItem/>
        </div>
        // {wishList}
    )
}